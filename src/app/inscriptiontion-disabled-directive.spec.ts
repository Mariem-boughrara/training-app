import { InscriptiontionDisabledDirective } from './inscriptiontion-disabled-directive';

describe('InscriptiontionDisabledDirective', () => {
  it('should create an instance', () => {
    const directive = new InscriptiontionDisabledDirective();
    expect(directive).toBeTruthy();
  });
});
